export type ChipItem = {
	/** Short text shown on the chip (typically ends with …). */
	label: string;
	/** Full prompt inserted into the input on select; also shown in the tooltip. */
	prompt: string;
};
